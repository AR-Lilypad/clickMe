import React from "react";
import "./style.css";

const Header = props => (
  <div className="headerBG fixed-top">
    <h2>of Butterflies, Birds, and Fish...</h2>
    <h4>Score with each click, lose if you click an animal twice!</h4>
  </div>
);

export default Header;
